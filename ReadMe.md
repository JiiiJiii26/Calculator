# Qt Calculator

A simple calculator application built with Qt framework featuring basic arithmetic operations, undo/redo functionality, and a clean user interface.

## Features

- Basic arithmetic operations (+, -, ×, ÷)
- Percentage calculations
- Sign change (+/-)
- Decimal point support
- Undo/Redo functionality
- Clear function

## Requirements

- Qt 5 or Qt 6
- CMake 3.16+
- C++17 compatible compiler

## Building

```bash
# Create build directory
mkdir build
cd build

# Configure with CMake
cmake ..

# Build
cmake --build .